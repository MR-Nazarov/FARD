confs_to_process: [44]

target: t1n
input: t1n_LR_4x
base_data_dir: /media/alex/drive2/DATASETS/MRI_deblur/BRATS/GLI/data/slices/test

base_result_dir:  /media/alex/DATA/AlexN/old_env/ALex/code/ablations

metrics: ['SSIM','PSNR','FSIM','LPIPS']


slices: [30,40]

use_npy: true

transforms:
  transpose:
    keys: ['output']
    dims: [0,2,1]
  flip:
     keys: [ 'output' ]
     dims: [ 1 ]  # Horizontal flip only (left-right)

  crop: [150,140]
  #brain_bbox: 2
