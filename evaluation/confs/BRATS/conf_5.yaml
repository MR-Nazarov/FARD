confs_to_process: [901]
#specific_cases: [1]
target: t1n
#input: t1n_LR_4x
base_data_dir: /media/alex/drive2/DATASETS/MRI_deblur/BRATS/GLI/data/slices/test

base_result_dir:  /media/alex/DATA/AlexN/old_env/ALex/code/BRATS

metrics: ['SSIM','PSNR','FSIM','LPIPS']


slices: [20,19]
enable_transform_visualization: false
enable_slice_preview: true
use_npy: true
print_specific_slices: false  # Set true to save per-slice images and print per-slice metric table
#specific_slice_indices: [10, 20, 30]  # Optional: process only these absolute slice indices


transforms:
   # Step 1: Clip values to valid range
   clip_values:
     min_val: 0.0
     max_val: 1.0
     keys: ['output', 'input', 'target']

   # Step 2: Transform output
#   transpose:
#     keys: ['output']
#     dims: [0,2,1]
   flip:
     keys: [ 'output' ]
     dims: [1]  # Horizontal flip only (left-right)

   # Step 3: Crop output to match target size
#   crop_to_match_target:
#     reference_key: 'target'
#     target_key: 'output'
#     mode: 'center'

   crop: [120,150]
  #brain_bbox: 2
