# Direct dependencies, pinned to the versions this code is verified against
# (Python 3.11.11, CUDA 12.8). Derived by walking every import in the repo rather
# than freezing an environment -- a freeze of the original env gave 200+ lines,
# nearly all transitive or unrelated.

--extra-index-url https://download.pytorch.org/whl/cu128

# --- core ---
torch==2.9.1
torchvision==0.24.1
numpy==1.26.4
PyYAML==6.0.2
munch==2.5.0
tqdm==4.67.1

# --- medical imaging ---
monai==1.4.0
SimpleITK==2.4.1
pydicom==3.0.1          # 3.x removed read_file/write_file; this code uses dcmread/dcmwrite
nibabel==5.3.2
torchio==0.20.3

# --- registration (data_prep/) ---
itk-elastix==0.25.4     # replaces SimpleElastix: sitk.ElastixImageFilter exists only in
                        # that build, which has no installable release. Same elastix
                        # engine underneath, so registration results are unchanged.

# --- metrics and losses ---
torchmetrics==1.4.0.post0
pytorch-msssim==1.0.0
lpips==0.1.4
scikit-image==0.25.0
scikit-learn==1.5.2
scipy==1.15.1

# --- model components ---
einops==0.8.0
kornia==0.8.0

# --- io, plotting, logging ---
pandas==2.2.3
matplotlib==3.10.0
pillow==12.2.0
opencv-python==4.10.0
imageio==2.33.1
dill==0.3.8
joblib==1.4.2
pathlib2==2.3.6
wandb==0.19.4           # optional; disabled by default (dontLog in confs/defaults.yaml)
